# ===================================================
# alias
# ===================================================

alias=dev-environment-version:
  tasks:
  - buildingblock-version
  annotations:
    description: "->[alias] generate version informations"
